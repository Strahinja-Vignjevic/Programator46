
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int i,j,k=100;
    char* nizcina = (char*) malloc(k * sizeof(char));
    printf("Unesite string: ");
    gets(nizcina);
    j=32;
    i=0;
    for(i=0;i<strlen(nizcina);i++)
    {
        if(nizcina[i]>= 'a' && nizcina[i]<='z')
        {
            niz[i]= toupper(nizcina[i]);
        }
    }
    puts(nizcina);
    free(nizcina);
    return 0;
}
